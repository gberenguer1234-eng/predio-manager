services:
  - type: web
    name: predio-manager
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TURSO_DB_URL
        sync: false
      - key: TURSO_AUTH_TOKEN
        sync: false
      - key: NODE_ENV
        value: production
